apiVersion: apps/v1
kind: Deployment
metadata:
  name: 3-tier-app
  namespace: qa
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 3-tier-app
  template:
    metadata:
      labels:
        app: 3-tier-app
    spec:
      imagePullSecrets:
      - name: 3-tier-app-reg-secret
      containers:
      - name: 3-tier-app
        image: iamridoydey/3-tier-app:13ed8127c86c27935139df13bb559cfabb84a564
        ports:
        - containerPort: 5000
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
        env:
        - name: DB_HOST
          value: mysql
        - name: DB_NAME
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_DATABASE
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_PASSWORD
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: DATABASE_URL
